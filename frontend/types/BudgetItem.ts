export interface BudgetItem {
    id: string;
    user_id: string;
    category: string;
    frequency: string;
    amount: string;
    spent: string;
    remaining: string;
}

export default BudgetItem